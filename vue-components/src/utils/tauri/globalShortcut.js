import {
  isRegistered,
  register,
  registerAll,
  unregister,
  unregisterAll,
} from '@tauri-apps/api/globalShortcut';

export { isRegistered, register, registerAll, unregister, unregisterAll };

export default {
  isRegistered,
  register,
  registerAll,
  unregister,
  unregisterAll,
};
